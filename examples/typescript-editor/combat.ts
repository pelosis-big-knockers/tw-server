// A second setup member, in a different file, so cross-file go-to-definition is
// demonstrable: ctrl+clicking `setup.attack` elsewhere jumps here.
setup.attack = (power: number): number => power * 2;
